# Config Repositories

<%= partial 'includes/shared/extension-info.md.erb', locals: { extension_name: 'configrepo', extension_version: '3.0', gocd_version: '20.8.0'} %>

## Introduction
Config repository extension endpoint allows a plugin to externalize pipeline and environment configuration. This allows
the configuration to be maintained and versioned outside of GoCD.
